import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primaryLight = Color(0xFF4CAF50);  // Green
  static const Color primaryDark = Color(0xFF388E3C);   // Darker Green

  // Secondary colors
  static const Color secondaryLight = Color(0xFFFFC107);  // Amber
  static const Color secondaryDark = Color(0xFFFFA000);   // Darker Amber

  // Background colors
  static const Color backgroundLight = Color(0xFFF5F5F5);  // Very Light Grey
  static const Color backgroundDark = Color(0xFF303030);   // Dark Grey

  // Surface colors
  static const Color surfaceLight = Colors.white;
  static const Color surfaceDark = Color(0xFF424242);  // Slightly lighter than background dark

  // Text colors
  static const Color textLight = Color(0xFF212121);  // Almost Black
  static const Color textDark = Colors.white;

  // Error colors
  static const Color errorLight = Color(0xFFD32F2F);  // Red
  static const Color errorDark = Color(0xFFFF5252);   // Lighter Red for dark theme

  // Budget-specific colors
  static const Color budgetPositive = Color(0xFF4CAF50);  // Green
  static const Color budgetNegative = Color(0xFFD34040);  // Red

  // Progress bar background
  static const Color progressBarBackground = Color(0xFFE0E0E0);  // Light Grey

  // You can add more color definitions as needed
}